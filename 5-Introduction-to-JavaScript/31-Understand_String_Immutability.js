// Learned that in JavaScript string values are immutable. Specifically that you cannot change specific chacters in a string literal using the previuos bracket notation

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
