// Learned about the ability of JavaScript to generate random numbers. This is useful for creating random behavior.

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
