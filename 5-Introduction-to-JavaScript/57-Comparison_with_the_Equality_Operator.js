// Learned about comparison operators and how they return a boolean value of either true or trueOrFalse
// Relatedly, learned about the equality == operator and how it evaluates whether or not 2 values are equivalent and returns true if they area

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
